# Contracts 合約資料

由 `/read-raw` 識別為合約 / 法律文件的 raw 進入此處（v1.1 新增分流路徑）。
存放：
- 客戶 SLA 條款
- 採購合約
- 標案文件節錄

格式：每份合約獨立檔案 `{對方-客戶名}_{合約類型}_{YYYY-MM-DD}.md`。
**注意**：本目錄含商業敏感資料，gitignore 規則應排除（透過 `.gitignore` 或加密儲存）。
